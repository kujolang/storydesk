# StoryDesk next-session worklist

- [x] Add an opt-in SQLite storage adapter after a benchmark proves a meaningful large-house advantage over immutable JSON records.
- [x] Add policy-configurable transition graphs for organizations whose editorial states differ from the built-in portable baseline.
- [x] Add signed export bundles for cross-host handoff without turning signing keys into a baseline dependency.
- [x] Add resumable packet generation for houses exceeding the current 1,000-record bounded query window.
- [x] Add adapter conformance fixtures for optional identity and scheduling integrations.
- [x] Run multi-process contention benchmarks on Linux, macOS, and Windows launch environments through the required CI matrix; macOS local evidence is recorded in `docs/BENCHMARKS.md` and all three launch artifacts are retained by CI.
