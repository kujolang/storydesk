# StoryDesk next-session worklist

- [ ] Add an opt-in SQLite storage adapter after a benchmark proves a meaningful large-house advantage over immutable JSON records.
- [ ] Add policy-configurable transition graphs for organizations whose editorial states differ from the built-in portable baseline.
- [ ] Add signed export bundles for cross-host handoff without turning signing keys into a baseline dependency.
- [ ] Add resumable packet generation for houses exceeding the current 1,000-record bounded query window.
- [ ] Add adapter conformance fixtures for optional identity and scheduling integrations.
- [ ] Run multi-process contention benchmarks on Linux, macOS, and Windows launch environments.
