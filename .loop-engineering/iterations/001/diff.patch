diff --git a/CHANGELOG.md b/CHANGELOG.md
index 546877a..7ee574b 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -1,5 +1,12 @@
 # Changelog
 
+## Unreleased
+
+- Refactored the runtime into focused Kujo modules under `src/`.
+- Added command-specific editorial validation, JSON configuration, bounded pagination, atomic export, structured error codes, and detailed doctor/validate reports.
+- Hardened storage with per-record write locks, secret-field rejection, traversal and symlink protection, resource ceilings, and append-only audit events.
+- Added domain and security suites, pinned-runtime CI, a one-command validation gate, and production-readiness documentation.
+
 ## 0.1.0 - 2026-08-14
 
 - Initial Kujo-native release with working local records, validation, contracts, fixtures, and safety boundaries.
diff --git a/README.md b/README.md
index 9b51644..15aceb1 100644
--- a/README.md
+++ b/README.md
@@ -1,15 +1,144 @@
 # StoryDesk
 
-Editorial control desk for ideas, campaigns, commissions, assignments, dependencies, packets, handoffs, and review queues.
+[![Version](https://img.shields.io/badge/version-0.1.0-black)](VERSION)
+[![CI](https://github.com/kujolang/storydesk/actions/workflows/validate.yml/badge.svg)](https://github.com/kujolang/storydesk/actions/workflows/validate.yml)
+[![License](https://img.shields.io/badge/license-MIT-lightgrey)](LICENSE)
+[![built with Kujo](https://img.shields.io/badge/built%20with-Kujo-white.svg)](https://github.com/kujolang/kujo)
 
-StoryDesk 0.1.0 is an independently installable, local-first Kujo tool. It requires no hosted service, Chain of Command, WebOps, or sibling Publishing House tool. The canonical entrypoint is `storydesk.kujo`; `bin/storydesk` contains no product logic.
+StoryDesk is a local-first editorial control desk for ideas, campaigns,
+commissions, assignments, dependencies, packets, handoffs, and human review
+queues. It is implemented in [Kujo](https://github.com/kujolang/kujo) and works
+without a hosted service, database server, model key, or sibling Publishing
+House tool.
 
-## CLI
+## Readiness posture
 
-Commands: idea add; idea list; campaign create; commission create; assign; packet daily; packet range; claim; status; block; handoff; review-queue; show; export; doctor; version; init; validate. Run `./bin/storydesk help` for flags. Mutations require `--actor`; JSON input uses `--input`. Common flags include `--json`, `--dry-run`, `--state`, `--output`, `--config`, and `--force`. Exit codes: 0 success, 1 validation/operation failure, 2 usage error.
+StoryDesk is ready for serious standalone editorial operations with immutable
+records, append-only audit events, atomic writes, per-record concurrency locks,
+bounded queries, deterministic fixtures, structured errors, and explicit
+authority boundaries. “Enterprise grade” is treated as a continuously verified
+standard—not a marketing label. It does not claim distributed multi-host
+coordination, hosted identity, or publication authority.
 
-State defaults to `.storydesk/`. Immutable JSON records and append-only history use atomic writes. IDs reject traversal; symlinks and oversized inputs are rejected. See [contracts](docs/contracts.md), [security](docs/security.md), and [quickstart](examples/quickstart.md).
+See the evidence-backed [production review](docs/PRODUCTION_READINESS_REVIEW.md)
+and [next-session worklist](docs/NEXT_SESSION.md).
 
-Test with `/Users/robertdevore/2026/Kujolang/kujo-repos/kujo/target/release/kujo run tests/test.kujo`, then run `./bin/storydesk doctor --json`.
+## Quick install
 
-0.1.0 covers the documented local records, fixtures, validation, checksums, deterministic fixed-time IDs, and structured export. It does not manufacture human judgment, consent, rights, approval, or causation. No publication APIs; recurring generation uses deterministic inputs and duplicate rejection.
+StoryDesk requires Kujo 1.0.1 or newer.
+
+```bash
+git clone https://github.com/kujolang/storydesk.git
+cd storydesk
+export KUJO_BIN=/absolute/path/to/kujo
+export PATH="$PWD/bin:$PATH"
+storydesk --version --json
+storydesk doctor --json
+```
+
+The launcher prefers `KUJO_BIN` and otherwise uses the verified ecosystem
+runtime path. Direct invocation remains available:
+
+```bash
+kujo run storydesk.kujo -- doctor --json
+```
+
+## Quick start
+
+```bash
+storydesk init --state .storydesk --json
+storydesk idea add \
+  --input fixtures/core.json \
+  --actor commissioning-editor \
+  --timestamp 2026-08-14T12:00:00Z \
+  --json
+storydesk idea list --limit 25 --json
+storydesk validate --json
+```
+
+Use a small JSON config when stable automation defaults are useful:
+
+```json
+{
+  "state": ".storydesk",
+  "actor": "managing-editor",
+  "limit": 100
+}
+```
+
+```bash
+storydesk idea list --config storydesk.json --after idea-previous --json
+```
+
+## Commands
+
+| Command | Purpose |
+| --- | --- |
+| `init` | Initialize portable local state. |
+| `idea add`, `idea list` | Capture and query the idea queue. |
+| `campaign create` | Record a campaign and objective. |
+| `commission create` | Create a validated editorial brief. |
+| `assign`, `claim` | Assign and claim bounded work. |
+| `status`, `block` | Append validated status or blocking-evidence events. |
+| `handoff` | Record explicit ownership transfer and next action. |
+| `packet daily`, `packet range` | Create deterministic work-packet records. |
+| `review-queue` | Query work awaiting human review. |
+| `show`, `history` | Inspect immutable records and audit-oriented listings. |
+| `export` | Emit or atomically write a bounded portable JSON bundle. |
+| `validate`, `doctor`, `version` | Verify records, environment health, and compatibility. |
+
+## Common flags
+
+| Flag | Behavior |
+| --- | --- |
+| `--state PATH` | Explicit local state directory. Traversal and symlinks are rejected. |
+| `--config FILE` | JSON defaults for `state`, `actor`, and `limit`. |
+| `--input FILE` | Command payload, limited to 1 MiB. |
+| `--actor ID` | Required identity for every mutation. |
+| `--timestamp UTC` | RFC 3339 UTC time; enables deterministic fixture IDs. |
+| `--id ID` | Explicit stable, type-prefixed record ID. |
+| `--type`, `--after`, `--limit` | Bounded filtering and cursor pagination. |
+| `--output FILE` | Atomic JSON export, never implicit overwrite. |
+| `--force` | Permits only explicitly named safe export replacement. |
+| `--dry-run` | Run validation and record construction without writes. |
+| `--json` | Stable `ok/data/error/error_code/tool_version/contract_version` envelope. |
+
+Exit codes are `0` for success, `1` for validated operational failure, and `2`
+for CLI usage errors.
+
+## Storage and security
+
+State defaults to `.storydesk/`. Records are immutable JSON objects under
+`records/`; audit events are append-only under `history/`; short-lived
+per-record lock directories prevent concurrent duplicate writes. Inputs,
+artifacts, list results, and exports have hard resource ceilings. Secret-shaped
+fields, traversal, symlink records, malformed JSON, incompatible schema majors,
+duplicate IDs, and unsafe overwrites fail closed.
+
+StoryDesk operates under OBSERVE and PROPOSE. It never manufactures approval,
+silently resolves evidence failures, calls publication APIs, or marks work
+published because a file exists. See [contracts](docs/contracts.md) and the
+[security model](docs/security.md).
+
+## Project structure
+
+```text
+storydesk.kujo        canonical entrypoint
+src/                  CLI, domain, contracts, storage, and shared Kujo modules
+tests/                Kujo regression, domain, and security suites
+schemas/              public JSON contracts
+fixtures/             deterministic offline inputs
+scripts/              validation and release-development gates
+docs/                 architecture, security, review, and future work
+bin/storydesk         logic-free POSIX launcher
+```
+
+## Verification
+
+```bash
+bash scripts/validate.sh
+```
+
+The gate pins the tested runtime in CI, runs every Kujo suite, validates JSON
+artifacts and CLI smoke paths, rejects foreign runtime dependencies, and checks
+the Git diff. Hosted providers are optional; none are required for the core.
diff --git a/cli.kujo b/cli.kujo
deleted file mode 100644
index e019b06..0000000
--- a/cli.kujo
+++ /dev/null
@@ -1,74 +0,0 @@
-func normalize_flag_name(token) {
-    if starts_with(token, "--") {
-        return substring(token, 2, len(token))
-    }
-    return token
-}
-
-func contains_name(names, name) {
-    mut i := 0
-    while i < len(names) {
-        if names[i] == name {
-            return true
-        }
-        i = i + 1
-    }
-    return false
-}
-
-func parse_args(tokens, bool_flags) {
-    mut flags := {}
-    mut positionals := []
-    mut errors := []
-    mut i := 0
-    while i < len(tokens) {
-        token := tokens[i]
-        if token == "--" {
-            i = i + 1
-            while i < len(tokens) {
-                positionals = push(positionals, tokens[i])
-                i = i + 1
-            }
-            continue
-        }
-        if starts_with(token, "--") {
-            eq_index := index_of(token, "=")
-            if eq_index >= 0 {
-                name := normalize_flag_name(substring(token, 0, eq_index))
-                if contains_name(bool_flags, name) {
-                    errors = push(errors, "--" + name + " does not take a value")
-                } else if has_key(flags, name) == 1 {
-                    errors = push(errors, "duplicate flag: --" + name)
-                } else {
-                    flags[name] := substring(token, eq_index + 1, len(token))
-                }
-                i = i + 1
-                continue
-            }
-            name := normalize_flag_name(token)
-            if has_key(flags, name) == 1 {
-                errors = push(errors, "duplicate flag: --" + name)
-                i = i + 1
-                continue
-            }
-            if contains_name(bool_flags, name) {
-                flags[name] := true
-                i = i + 1
-                continue
-            }
-            if i + 1 < len(tokens) && starts_with(tokens[i + 1], "--") == false {
-                flags[name] := tokens[i + 1]
-                i = i + 2
-                continue
-            }
-            flags[name] := ""
-            i = i + 1
-            continue
-        }
-        positionals = push(positionals, token)
-        i = i + 1
-    }
-    return {"flags": flags, "positionals": positionals, "errors": errors}
-}
-
-export parse_args := parse_args
diff --git a/fixtures/core.json b/fixtures/core.json
index cbb633c..3db29f3 100644
--- a/fixtures/core.json
+++ b/fixtures/core.json
@@ -1 +1,11 @@
-{"schema_version":"1.0.0","title":"StoryDesk fixture","provenance":[{"kind":"fixture","source":"fixtures/core.json"}],"classification":"observed","verdict":"unverified","blocking_findings":[],"human_actor":"fixture-human","package_id":"package-fixture","package_version":"1","manifest_checksum":"fixture-checksum","destination":"fixture-local","action":"publish","approval_id":"approval-fixture","authorized_action":"publish"}
+{
+  "schema_version": "1.0.0",
+  "title": "Portable editorial workflow",
+  "detail": "A deterministic offline idea fixture for StoryDesk.",
+  "provenance": [
+    {
+      "kind": "fixture",
+      "source": "fixtures/core.json"
+    }
+  ]
+}
diff --git a/src/core.kujo b/src/core.kujo
index 3a1b183..addbf67 100644
--- a/src/core.kujo
+++ b/src/core.kujo
@@ -1,42 +1,169 @@
-from cli import parse_args
-SLUG:="storydesk";NAME:="StoryDesk";VERSION:="0.1.0";CONTRACT:="1.0.0";DEFAULT_STATE:=".storydesk";PREFIX:="storydesk";COMMANDS:=split("idea add,idea list,campaign create,commission create,assign,packet daily,packet range,claim,status,block,handoff,review-queue,show,export,doctor,version,init,validate",",")
-func get(d,k,f){if type(d)=="dict"&&has_key(d,k)==1{return d[k]};return f}
-func has(a,v){for x in a{if x==v{return true}};return false}
-func flag(p,k,f){if has_key(p["flags"],k)==1{return p["flags"][k]};return f}
-func safe(v){return type(v)=="string"&&v!=""&&v!="."&&v!=".."&&contains(v,"/")==0&&contains(v,"\\")==0}
-func parent(p){mut i:=len(p)-1;while i>=0{if substring(p,i,i+1)=="/"{if i==0{return "/"};return substring(p,0,i)};i=i-1};return "."}
-func tree(p){if p==""||p=="."{return};parts:=split(p,"/");cur:="";if starts_with(p,"/"){cur="/"};for part in parts{if part!=""&&part!="."{if cur=="/"{cur="/"+part}else if cur==""{cur=part}else {cur=cur+"/"+part};if !path_is_dir(cur){create_dir(cur)}}}}
-func atomic(p,t){tree(parent(p));tmp:=p+".tmp-"+to_string(now());write_file(tmp,t,true);rename_file(tmp,p)}
-func canonical(v){if type(v)=="dict"{a:=[];for k in sort(keys(v)){a=push(a,to_json(k)+":"+canonical(v[k]))};return "{"+join(a,",")+"}"};if type(v)=="array"{a:=[];for x in v{a=push(a,canonical(x))};return "["+join(a,",")+"]"};return to_json(v)}
-func state(p){return flag(p,"state",DEFAULT_STATE)}
-func path(p,id){return join_path(join_path(state(p),"records"),id+".json")}
-func init(p){d:=state(p);if file_exists(d)&&path_is_symlink(d){return {"ok":false,"error":"state directory symlinks are not allowed"}};tree(join_path(d,"records"));tree(join_path(d,"history"));m:=join_path(d,"metadata.json");if !path_is_file(m){atomic(m,to_json_pretty({"schema_version":"1.0.0","tool":SLUG,"tool_version":VERSION,"created_at":now_utc()}))};return {"ok":true,"value":{"initialized":true,"state":d}}}
-func parse_object(text){mut v:=null;try {v=parse_json(text)} except err {return {"ok":false,"error":"malformed JSON"}};if type(v)!="dict"{return {"ok":false,"error":"input must be a JSON object"}};return {"ok":true,"value":v}}
-func payload(p){f:=flag(p,"input","");if f==""{return {"ok":true,"value":{}}};if !path_is_file(f){return {"ok":false,"error":"input file not found"}};if path_is_symlink(f)||file_size(f)>1048576{return {"ok":false,"error":"unsafe or oversized input"}};parsed:=parse_object(read_file(f));if !parsed["ok"]{return parsed};v:=parsed["value"];s:=get(v,"schema_version","1.0.0");if type(s)!="string"||!starts_with(s,"1."){return {"ok":false,"error":"incompatible schema_version"}};return {"ok":true,"value":v}}
-func domain(c,v,p){if SLUG=="dossier"&&(c=="evidence classify"||c=="verify"){cl:=get(v,"classification","");if cl!=""&&!has(["verified","observed","inferred","opinion","hypothesis","planned","conflicted","expired","unavailable","rejected"],cl){return "invalid evidence classification"};if cl=="verified"&&get(v,"captured_support","")==""{return "verified evidence requires captured support"}};if SLUG=="bluepencil"&&c=="review"{ver:=get(v,"verdict","unverified");if !has(["pass","pass_with_queries","revise","blocked","reject","unverified"],ver){return "invalid verdict"};if len(get(v,"blocking_findings",[]))>0&&(ver=="pass"||ver=="pass_with_queries"){return "blockers cannot be averaged away"}};if SLUG=="versionseal"&&c=="approve"{for k in ["human_actor","package_id","package_version","manifest_checksum","destination","action"]{if get(v,k,"")==""{return "approval missing "+k}};if flag(p,"actor","")!=v["human_actor"]{return "actor must match human_actor"}};if SLUG=="presswire"&&has(["publish","schedule","correct","unpublish"],c){if flag(p,"act",false)!=true||flag(p,"yes",false)!=true{return "effects require --act --yes"};if get(v,"approval_id","")==""||get(v,"manifest_checksum","")==""{return "approval evidence required"};if get(v,"authorized_action","")!=c{return "approval action mismatch"}};return ""}
-func kind(c){a:=split(c," ");return a[0]}
-func idprefix(c){if SLUG=="storydesk"{if kind(c)=="idea"{return "idea"};if kind(c)=="campaign"{return "campaign"};if kind(c)=="commission"{return "brief"};if c=="assign"{return "assignment"};if c=="handoff"{return "handoff"}};if SLUG=="dossier"&&has(["claim","source","evidence","rights"],kind(c)){return kind(c)};if SLUG=="galleypack"{if c=="build"||c=="freeze"{return "package"};return "artifact"};if SLUG=="bluepencil"{return "review"};if SLUG=="versionseal"{return "approval"};if SLUG=="presswire"{return "publication"};if SLUG=="readersignal"{if c=="learn"||c=="followup"{return "learning"};return "measurement"};if SLUG=="assetworks"{return "asset"};return PREFIX}
-func create(c,p){i:=init(p);if !i["ok"]{return i};x:=payload(p);if !x["ok"]{return x};v:=x["value"];e:=domain(c,v,p);if e!=""{return {"ok":false,"error":e}};actor:=flag(p,"actor","");if actor==""{return {"ok":false,"error":"mutating commands require --actor"}};ts:=flag(p,"timestamp",now_utc());pre:=idprefix(c);id:=flag(p,"id","");if id==""{id=pre+"-"+substring(sha256(SLUG+"\n"+c+"\n"+actor+"\n"+ts+"\n"+canonical(v)),0,20)};if !safe(id)||!starts_with(id,pre+"-"){return {"ok":false,"error":"invalid id prefix"}};dest:=path(p,id);if path_is_file(dest){return {"ok":false,"error":"duplicate record id"}};r:={"schema_version":"1.0.0","tool_version":VERSION,"id":id,"record_type":kind(c),"command":c,"actor":actor,"created_at":ts,"provenance":get(v,"provenance",[]),"payload":v};artifact:=flag(p,"path","");if artifact!=""{if !path_is_file(artifact)||path_is_symlink(artifact)||file_size(artifact)>67108864{return {"ok":false,"error":"unsafe artifact"}};r["artifact"]={"path":artifact,"sha256":sha256_file(artifact),"bytes":file_size(artifact)}};if flag(p,"dry-run",false)==true{return {"ok":true,"value":{"dry_run":true,"record":r}}};atomic(dest,to_json_pretty(r));atomic(join_path(state(p),"history/"+substring(sha256(ts+id),0,24)+".json"),to_json_pretty({"schema_version":"1.0.0","event":"record.created","record_id":id,"actor":actor,"at":ts,"checksum":sha256(canonical(r))}));return {"ok":true,"value":r}}
-func load(p,id){if !safe(id){return {"ok":false,"error":"unsafe record id"}};f:=path(p,id);if !path_is_file(f)||path_is_symlink(f){return {"ok":false,"error":"record not found"}};v:=parse_json(read_file(f));if type(v)!="dict"{return {"ok":false,"error":"malformed record"}};return {"ok":true,"value":v}}
-func list(p){a:=[];d:=join_path(state(p),"records");if !path_is_dir(d){return a};for n in sort(list_dir(d)){if ends_with(n,".json")&&!path_is_symlink(join_path(d,n)){v:=parse_json(read_file(join_path(d,n)));if type(v)=="dict"{a=push(a,v)};if len(a)>=1000{return a}}};return a}
-func verify(p){l:=load(p,flag(p,"id",""));if !l["ok"]{return l};f:=flag(p,"path","");if f!=""{expected:=get(get(l["value"],"artifact",{}),"sha256",get(l["value"]["payload"],"manifest_checksum",""));if !path_is_file(f)||expected==""||sha256_file(f)!=expected{return {"ok":false,"error":"checksum drift detected"}}};return {"ok":true,"value":{"valid":true,"record_id":l["value"]["id"],"checksum":sha256(canonical(l["value"]))}}}
-export func dispatch(c,p){if c=="init"{return init(p)};if c=="version"{return {"ok":true,"value":{"tool":SLUG,"version":VERSION,"kujo_version":"1.0.1"}}};if c=="doctor"{return {"ok":true,"value":{"healthy":true,"state":state(p),"kujo_version":"1.0.1","limitations":"No publication APIs; recurring generation uses deterministic inputs and duplicate rejection."}}};if c=="show"||c=="claim show"||c=="receipt"||c=="inspect"{return load(p,flag(p,"id",""))};if has(["validate","verify","freshness check","status"],c){return verify(p)};if has(["list","idea list","review-queue","history","report","signals","adapters","capabilities","disagreements"],c){a:=list(p);return {"ok":true,"value":{"records":a,"count":len(a)}}};if c=="export"{return {"ok":true,"value":{"schema_version":"1.0.0","tool":SLUG,"records":list(p)}}};return create(c,p)}
-func cname(a){if len(a)==0{return "help"};if len(a)>1&&has(COMMANDS,a[0]+" "+a[1]){return a[0]+" "+a[1]};return a[0]}
-func tokens(a,c){if contains(c," ")==1{return slice(a,2,len(a))};return slice(a,1,len(a))}
-func env(ok,data,error){return {"ok":ok,"data":data,"error":error,"tool_version":VERSION,"contract_version":CONTRACT}}
-export func main(a) {
-    c := cname(a)
-    if c == "help" { print(NAME + " " + VERSION + "\n\nCommands:\n  " + join(COMMANDS, "\n  ") + "\n\nFlags: --state --input --id --actor --timestamp --path --json --dry-run --output --config --force"); return 0 }
-    if !has(COMMANDS, c) { eprint(NAME + ": unknown command: " + c); return 2 }
-    p := parse_args(tokens(a, c), ["json", "dry-run", "force", "act", "yes"])
-    if len(p["errors"]) > 0 { eprint(NAME + ": " + p["errors"][0]); return 2 }
-    allowed := ["state", "input", "id", "actor", "timestamp", "path", "json", "dry-run", "output", "config", "force", "act", "yes"]
-    for k in keys(p["flags"]) { if !has(allowed, k) { eprint(NAME + ": unknown flag --" + k); return 2 } }
-    r := dispatch(c, p)
-    out := env(r["ok"], get(r, "value", null), get(r, "error", null))
-    if flag(p, "json", false) { print(to_json_pretty(out)) }
-    else if r["ok"] { print(NAME + ": ok\n" + to_json_pretty(get(r, "value", null))) }
-    else { eprint(NAME + ": " + r["error"]) }
-    if r["ok"] { return 0 }
+from src.args import parse_args
+from src.common import get, has, canonical_json, parse_object, safe_id, valid_timestamp, find_sensitive_key, bounded_int, envelope
+from src.profile import profile
+from src.domain import record_type, id_prefix, validate_command
+from src.storage import initialize, save_new, load_record, list_records, validate_state_path
+
+func flag(parsed, name, fallback) {
+    if has_key(parsed["flags"], name) == 1 { return parsed["flags"][name] }
+    return fallback
+}
+
+func command_name(argv, commands) {
+    if len(argv) == 0 { return "help" }
+    if argv[0] == "--help" { return "help" }
+    if argv[0] == "--version" { return "version" }
+    if len(argv) > 1 && has(commands, argv[0] + " " + argv[1]) { return argv[0] + " " + argv[1] }
+    return argv[0]
+}
+
+func command_tokens(argv, command) {
+    if command == "help" || (len(argv) > 0 && starts_with(argv[0], "--")) { return [] }
+    if contains(command, " ") == 1 { return slice(argv, 2, len(argv)) }
+    return slice(argv, 1, len(argv))
+}
+
+func load_config(path) {
+    if path == "" { return {"ok": true, "value": {}} }
+    if !path_is_file(path) || path_is_symlink(path) { return {"ok": false, "code": "config_not_found", "error": "config must be a regular file"} }
+    if file_size(path) > 65536 { return {"ok": false, "code": "config_too_large", "error": "config exceeds 64 KiB"} }
+    config_result := parse_object(read_file(path), "config"); if !config_result["ok"] { return config_result }
+    for key in keys(config_result["value"]) { if !has(["state", "actor", "limit"], key) { return {"ok": false, "code": "unknown_config_key", "error": "unknown config key: " + key} } }
+    return config_result
+}
+
+func setting(parsed, config, name, fallback) {
+    value := flag(parsed, name, null)
+    if type(value) != "null" && value != "" { return value }
+    return get(config, name, fallback)
+}
+
+func read_payload(parsed) {
+    path := flag(parsed, "input", "")
+    if path == "" { return {"ok": true, "value": {"schema_version": "1.0.0"}} }
+    if !path_is_file(path) || path_is_symlink(path) { return {"ok": false, "code": "input_not_found", "error": "input must be a regular file"} }
+    if file_size(path) > 1048576 { return {"ok": false, "code": "input_too_large", "error": "input exceeds 1 MiB"} }
+    parsed_payload := parse_object(read_file(path), "input"); if !parsed_payload["ok"] { return parsed_payload }
+    payload := parsed_payload["value"]
+    schema := get(payload, "schema_version", "1.0.0")
+    if type(schema) != "string" || !starts_with(schema, "1.") { return {"ok": false, "code": "incompatible_schema", "error": "input schema major must be 1"} }
+    sensitive := find_sensitive_key(payload, "")
+    if sensitive != "" { return {"ok": false, "code": "secret_rejected", "error": "sensitive field is not allowed: " + sensitive} }
+    return {"ok": true, "value": payload}
+}
+
+func artifact_metadata(path) {
+    if path == "" { return {"ok": true, "value": null} }
+    if !path_is_file(path) || path_is_symlink(path) { return {"ok": false, "code": "unsafe_artifact", "error": "artifact must be a regular non-symlink file"} }
+    if file_size(path) > 67108864 { return {"ok": false, "code": "artifact_too_large", "error": "artifact exceeds 64 MiB"} }
+    return {"ok": true, "value": {"path": path, "sha256": sha256_file(path), "bytes": file_size(path)}}
+}
+
+func create_record(command, parsed, config, p) {
+    state := setting(parsed, config, "state", p["default_state"])
+    initialized := initialize(state, {"schema_version": "1.0.0", "tool": p["slug"], "tool_version": p["version"], "contract_version": p["contract_version"], "created_at": now_utc()})
+    if !initialized["ok"] { return initialized }
+    input := read_payload(parsed); if !input["ok"] { return input }; payload := input["value"]
+    actor := setting(parsed, config, "actor", "")
+    if type(actor) != "string" || trim(actor) == "" || len(actor) > 256 { return {"ok": false, "code": "actor_required", "error": "mutating commands require a bounded --actor or config actor"} }
+    timestamp := flag(parsed, "timestamp", now_utc())
+    if !valid_timestamp(timestamp) { return {"ok": false, "code": "invalid_timestamp", "error": "timestamp must be RFC 3339 UTC"} }
+    artifact := artifact_metadata(flag(parsed, "path", "")); if !artifact["ok"] { return artifact }
+    domain_error := validate_command(command, payload, artifact["value"])
+    if domain_error != "" { return {"ok": false, "code": "domain_validation", "error": domain_error} }
+    prefix := id_prefix(command); id := flag(parsed, "id", "")
+    if id == "" { id = prefix + "-" + substring(sha256(p["slug"] + "\n" + command + "\n" + actor + "\n" + timestamp + "\n" + canonical_json(payload)), 0, 20) }
+    if !safe_id(id) || !starts_with(id, prefix + "-") { return {"ok": false, "code": "unsafe_id", "error": "id must use prefix " + prefix + "- and contain only lowercase letters, digits, and hyphens"} }
+    record := {"schema_version": "1.0.0", "tool_version": p["version"], "contract_version": p["contract_version"], "id": id, "record_type": record_type(command), "command": command, "actor": actor, "created_at": timestamp, "provenance": get(payload, "provenance", []), "payload": payload}
+    if type(artifact["value"]) == "dict" { record["artifact"] = artifact["value"] }
+    if flag(parsed, "dry-run", false) == true { return {"ok": true, "value": {"dry_run": true, "record": record}} }
+    stored := save_new(state, record); if !stored["ok"] { return stored }
+    return {"ok": true, "value": {"record": record, "storage": stored["value"]}}
+}
+
+func validate_record(record, p) {
+    for key in ["schema_version", "tool_version", "contract_version", "id", "record_type", "command", "actor", "created_at", "provenance", "payload"] {
+        if has_key(record, key) == 0 { return {"ok": false, "code": "missing_field", "error": "record missing field: " + key} }
+    }
+    if record["schema_version"] != "1.0.0" || record["contract_version"] != p["contract_version"] { return {"ok": false, "code": "incompatible_record", "error": "record contract is incompatible"} }
+    if !safe_id(record["id"]) || !valid_timestamp(record["created_at"]) { return {"ok": false, "code": "invalid_record", "error": "record id or timestamp is invalid"} }
+    if type(record["payload"]) != "dict" || type(record["provenance"]) != "array" { return {"ok": false, "code": "invalid_record", "error": "payload/provenance shape is invalid"} }
+    error := validate_command(record["command"], record["payload"], get(record, "artifact", null))
+    if error != "" { return {"ok": false, "code": "domain_validation", "error": error} }
+    return {"ok": true}
+}
+
+func export_result(value, parsed, p) {
+    output := flag(parsed, "output", "")
+    if output == "" { return {"ok": true, "value": value} }
+    if file_exists(output) && path_is_symlink(output) { return {"ok": false, "code": "unsafe_output", "error": "output symlinks are not allowed"} }
+    if file_exists(output) && flag(parsed, "force", false) != true { return {"ok": false, "code": "output_exists", "error": "output exists; use --force only for intentional safe replacement"} }
+    rendered := to_json_pretty(envelope(true, value, null, null, p["version"], p["contract_version"])) + "\n"
+    if len(rendered) > 8388608 { return {"ok": false, "code": "output_too_large", "error": "output exceeds 8 MiB"} }
+    write_file_atomic(output, rendered, true)
+    return {"ok": true, "value": {"output": output, "bytes": len(rendered), "sha256": sha256(rendered)}}
+}
+
+export func dispatch(command, parsed) {
+    p := profile(); config_result := load_config(flag(parsed, "config", "")); if !config_result["ok"] { return config_result }; config := config_result["value"]
+    state := setting(parsed, config, "state", p["default_state"])
+    if command == "init" { return initialize(state, {"schema_version": "1.0.0", "tool": p["slug"], "tool_version": p["version"], "contract_version": p["contract_version"], "created_at": now_utc()}) }
+    if command == "version" { return {"ok": true, "value": {"tool": p["slug"], "version": p["version"], "contract_version": p["contract_version"], "minimum_kujo": "1.0.1"}} }
+    if command == "doctor" {
+        state_error := validate_state_path(state); listing := {"records": [], "warnings": [], "truncated": false}
+        if state_error == "" { listing = list_records(state, "", "", 1000) }
+        healthy := state_error == "" && len(listing["warnings"]) == 0
+        mut code := null; mut error := null
+        if state_error != "" { code = "unsafe_state"; error = state_error }
+        if state_error == "" && len(listing["warnings"]) > 0 { code = "corrupt_state"; error = "one or more records are unreadable" }
+        return {"ok": healthy, "code": code, "error": error, "value": {"healthy": healthy, "tool": p["slug"], "version": p["version"], "minimum_kujo": "1.0.1", "state": state, "record_count": len(listing["records"]), "corrupt_records": listing["warnings"], "limits": {"input_bytes": 1048576, "record_bytes": 1048576, "artifact_bytes": 67108864, "list_records": 1000}, "limitations": p["limitations"]}}
+    }
+    if command == "show" {
+        id := flag(parsed, "id", ""); if id == "" { return {"ok": false, "code": "id_required", "error": "show requires --id"} }
+        return load_record(state, id)
+    }
+    if command == "idea list" || command == "review-queue" || command == "history" {
+        limit_result := bounded_int(setting(parsed, config, "limit", 100), 100, 1, 1000); if !limit_result["ok"] { return limit_result }
+        kind := flag(parsed, "type", ""); if command == "idea list" { kind = "idea" }
+        listing := list_records(state, kind, flag(parsed, "after", ""), limit_result["value"])
+        return {"ok": true, "value": listing}
+    }
+    if command == "validate" {
+        id := flag(parsed, "id", "")
+        if id != "" { loaded := load_record(state, id); if !loaded["ok"] { return loaded }; checked := validate_record(loaded["value"], p); if !checked["ok"] { return checked }; return {"ok": true, "value": {"valid": true, "id": id, "checksum": loaded["checksum"]}} }
+        listing := list_records(state, "", "", 1000); mut invalid := []
+        for record in listing["records"] { checked := validate_record(record, p); if !checked["ok"] { invalid = push(invalid, {"id": record["id"], "code": checked["code"], "error": checked["error"]}) } }
+        valid := len(invalid) == 0 && len(listing["warnings"]) == 0
+        mut code := null; mut error := null
+        if !valid { code = "validation_failed"; error = "one or more records failed validation" }
+        return {"ok": valid, "code": code, "error": error, "value": {"valid": valid, "checked": len(listing["records"]), "invalid": invalid, "storage_warnings": listing["warnings"], "truncated": listing["truncated"]}}
+    }
+    if command == "export" {
+        limit_result := bounded_int(setting(parsed, config, "limit", 1000), 1000, 1, 1000); if !limit_result["ok"] { return limit_result }
+        listing := list_records(state, flag(parsed, "type", ""), flag(parsed, "after", ""), limit_result["value"])
+        return export_result({"schema_version": "1.0.0", "tool": p["slug"], "records": listing["records"], "warnings": listing["warnings"], "truncated": listing["truncated"]}, parsed, p)
+    }
+    return create_record(command, parsed, config, p)
+}
+
+func usage(p) {
+    return p["name"] + " " + p["version"] + "\n\n" + p["description"] + "\n\nCommands:\n  " + join(p["commands"], "\n  ") + "\n\nCommon flags:\n  --state PATH       Local state directory\n  --config FILE      JSON defaults for state, actor, and limit\n  --input FILE       JSON command payload (1 MiB maximum)\n  --actor ID         Required for mutations\n  --timestamp UTC    Deterministic RFC 3339 timestamp\n  --id ID            Explicit stable record ID\n  --path FILE        Bounded artifact attachment\n  --type TYPE        Record filter for list/export\n  --after ID         Pagination cursor\n  --limit N          1..1000 records\n  --output FILE      Atomic JSON export\n  --force            Replace only an explicitly named safe output\n  --dry-run          Validate without writing\n  --json             Stable output envelope"
+}
+
+export func main(argv) {
+    p := profile(); command := command_name(argv, p["commands"])
+    if command == "help" { print(usage(p)); return 0 }
+    if !has(p["commands"], command) { eprint(p["name"] + ": unknown command: " + command); return 2 }
+    parsed := parse_args(command_tokens(argv, command), ["json", "dry-run", "force"])
+    if len(parsed["errors"]) > 0 { eprint(p["name"] + ": " + parsed["errors"][0]); return 2 }
+    allowed := ["state", "config", "input", "id", "actor", "timestamp", "path", "type", "after", "limit", "output", "json", "dry-run", "force"]
+    for name in keys(parsed["flags"]) { if !has(allowed, name) { eprint(p["name"] + ": unknown flag --" + name); return 2 } }
+    if len(parsed["positionals"]) > 0 { eprint(p["name"] + ": unexpected positional argument: " + parsed["positionals"][0]); return 2 }
+    result := dispatch(command, parsed)
+    output := envelope(result["ok"], get(result, "value", null), get(result, "error", null), get(result, "code", null), p["version"], p["contract_version"])
+    if flag(parsed, "json", false) { print(to_json_pretty(output)) }
+    else if result["ok"] { print(p["name"] + ": ok\n" + to_json_pretty(get(result, "value", null))) }
+    else { eprint(p["name"] + ": " + get(result, "error", "operation failed") + " [" + get(result, "code", "unknown") + "]") }
+    if result["ok"] { return 0 }
     return 1
 }
diff --git a/tests/test.kujo b/tests/test.kujo
index f8887c3..68372f3 100644
--- a/tests/test.kujo
+++ b/tests/test.kujo
@@ -1,12 +1,12 @@
 from src.core import dispatch
-from cli import parse_args
+from src.args import parse_args
 mut pass:=0;mut fail:=0
 func check(ok,label){if ok{pass=pass+1}else{fail=fail+1;eprint("FAIL: "+label)}}
 root:="/tmp/storydesk-test-"+uuid_v4()
 func opts(v){return parse_args(v,["json","dry-run","force","act","yes"])}
 check(dispatch("init",opts(["--state",root]))["ok"],"init")
 created:=dispatch("idea add",opts(["--state",root,"--input","fixtures/core.json","--actor","fixture-human","--timestamp","2026-08-14T00:00:00Z"]))
-check(created["ok"],"core workflow");id:="";if created["ok"]{id=created["value"]["id"]}
+check(created["ok"],"core workflow");id:="";if created["ok"]{id=created["value"]["record"]["id"]}
 check(!dispatch("idea add",opts(["--state",root,"--input","fixtures/core.json","--actor","fixture-human","--timestamp","2026-08-14T00:00:00Z"]))["ok"],"duplicate")
 check(!dispatch("idea add",opts(["--state",root,"--input","fixtures/core.json"]))["ok"],"actor required")
 check(!dispatch("show",opts(["--state",root,"--id","../escape"]))["ok"],"traversal")
